- Loading data slide add a spacer before the first heading.

- Dataframe function slide add a heading "Defining dataframes in R" and use bullets to explain the coming function, then a second heading for the function itself.

- Constructing a dataframe slide add a print our of the resulting dataframe.

- Change the example -constructing a dataframe slide to a different and slightly more complex example.  Print the dataframe in the second half of the slide.
