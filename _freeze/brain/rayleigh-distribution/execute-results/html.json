{
  "hash": "04f247a7ae1cca5a521e5ca53d3e99c1",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: Rayleigh Distribution\nauthor: John Robin Inston\ndate: 2026-08-30\nnumber-sections: true\ntitle-block-banner: true\n---\n\n## Introduction\n\nThe **Rayleigh distribution** is a continuous probability distribution\nfor the magnitude of a two-dimensional vector whose components are\nindependent, zero-mean normal variables. It arises for wind speeds,\nsignal envelopes, and scattering amplitudes.\n\n:::{.definition data-title=\"Rayleigh Distribution\"}\n\nA random variable $X$ follows a **Rayleigh distribution** with scale\nparameter $\\sigma > 0$ if\n\n$$\nX \\sim \\text{Rayleigh}(\\sigma).\n$$\n\n:::\n\n## Probability Density Function\n\n:::{.definition data-title=\"Probability Density Function (PDF)\"}\n\nThe probability density function is\n\n$$\nf(x) = \\frac{x}{\\sigma^2}\n  \\exp\\!\\left(-\\frac{x^2}{2\\sigma^2}\\right),\n  \\quad x \\geq 0.\n$$\n\n:::\n\n*Interpretation:* The density vanishes at the origin, rises to a mode at\n$x = \\sigma$, and then decays like a Gaussian tail.\n\n::: {#4b6fe841 .cell execution_count=1}\n``` {.python .cell-code code-fold=\"true\" code-summary=\"Plotting code\"}\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport seaborn as sns\nfrom scipy.stats import rayleigh\n\nsns.set_style('whitegrid')\nsns.set_palette('Set2')\n\nx = np.linspace(0, 8, 400)\nfig, ax = plt.subplots(figsize=(7, 4))\nfor sigma in (0.5, 1.0, 2.0):\n    ax.plot(x, rayleigh.pdf(x, scale=sigma),\n            lw=2, label=rf'$\\sigma = {sigma}$')\n\nax.set_xlabel('$x$')\nax.set_ylabel('$f(x)$')\nax.set_title('Rayleigh density')\nax.legend()\nplt.show()\n```\n\n::: {.cell-output .cell-output-display}\n![Density of the Rayleigh distribution for several scale parameters $\\sigma$.](rayleigh-distribution_files/figure-html/cell-2-output-1.png){width=587 height=373}\n:::\n:::\n\n\n:::{.definition data-title=\"Cumulative Distribution Function (CDF)\"}\n\nThe cumulative distribution function is\n\n$$\nF(x) = 1 - \\exp\\!\\left(-\\frac{x^2}{2\\sigma^2}\\right),\n  \\quad x \\geq 0.\n$$\n\n:::\n\n## Key Properties\n\n:::{.theorem data-title=\"Moments of the Rayleigh Distribution\"}\n\nFor $X \\sim \\text{Rayleigh}(\\sigma)$,\n\n$$\n\\mathbb{E}[X] = \\sigma\\sqrt{\\frac{\\pi}{2}} \\quad \\& \\quad\n  \\operatorname{Var}(X) = \\frac{4 - \\pi}{2}\\sigma^2.\n$$\n\n:::\n\n:::{.proof}\n\nWith the substitution $u = x^2/(2\\sigma^2)$,\n\n$$\n\\mathbb{E}[X] = \\int_0^\\infty \\frac{x^2}{\\sigma^2}\n  e^{-x^2/(2\\sigma^2)} \\, dx\n  = \\sigma\\sqrt{\\frac{\\pi}{2}}.\n$$\n\nSince $\\mathbb{E}[X^2] = 2\\sigma^2$, the variance is\n$2\\sigma^2 - \\frac{\\pi}{2}\\sigma^2 = \\frac{4 - \\pi}{2}\\sigma^2$.\n\n:::\n\n## Relationship to Other Distributions\n\n- **[[normal-distribution]]:** If $U, V \\sim \\mathcal{N}(0, \\sigma^2)$\n  are independent, then $\\sqrt{U^2 + V^2} \\sim \\text{Rayleigh}(\\sigma)$.\n- **[[chi-squared-distribution]]:** $X^2/\\sigma^2 \\sim \\chi^2_2$, so\n  $X^2$ is exponentially distributed.\n- **[[weibull-distribution]]:** The Rayleigh is a Weibull distribution\n  with shape $k = 2$.\n- **[[maxwell-boltzmann-distribution]]:** The three-dimensional analogue,\n  for the magnitude of a 3D Gaussian vector.\n\n## Examples and Applications\n\n:::{.example data-title=\"Signal processing\"}\n\nThe envelope of a narrowband noise signal, and fading amplitudes in\nwireless communication, follow a Rayleigh distribution.\n\n:::\n\n:::{.example data-title=\"Wind speed\"}\n\nWind speed magnitudes are often modelled as Rayleigh, the special case of\nthe Weibull wind-speed model with shape $2$.\n\n:::\n\n:::{.example data-title=\"Target distances\"}\n\nThe distance from the origin to a point with independent normal\ncoordinate errors is Rayleigh distributed, relevant to targeting and\nnavigation.\n\n:::\n\n",
    "supporting": [
      "rayleigh-distribution_files"
    ],
    "filters": [],
    "includes": {}
  }
}